#include "mainwindow.h"

#include <QApplication>
#include <map>
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.show();
    return a.exec();
}
std::map<int,std::string> listOfNumbers;
for(const auto& number: listOfNumbers){
    if(number.second == surname)
}
